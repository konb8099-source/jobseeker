## Packages
framer-motion | Smooth animations for page transitions and interactions
lucide-react | Beautiful icons for the UI
clsx | Utility for constructing className strings conditionally
tailwind-merge | Utility for merging Tailwind classes efficiently

## Notes
- The application uses RTL (Right-to-Left) direction for Persian language support.
- API endpoints are defined in shared/routes.ts and must be strictly followed.
- Images for categories are simulated with high-quality Unsplash images to ensure a premium look immediately.
- Currency conversion is strictly "1 OMR = 2.6 USD" as per requirements.
